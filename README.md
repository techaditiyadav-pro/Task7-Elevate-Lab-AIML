# Task 7: Support Vector Machines (SVM)

This project demonstrates linear and non-linear SVM classification using the Breast Cancer dataset.

## Features
- Linear and RBF SVM
- Hyperparameter tuning with GridSearchCV
- Decision boundary visualization-ready setup
- Train-test split and scaling

## Files
- `svm.py` — main script
- `requirements.txt`

## How to Run
```bash
pip install -r requirements.txt
python svm.py
```
